rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client SDK access. contactMessages and blogPosts are Admin SDK only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
