rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client SDK access. contactMessages is Admin SDK only (app/api/contact).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
